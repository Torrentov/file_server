//all is done
from django.db import models


class Document(models.Model):
    folder = files/needed_files/folder2/"another one"/folder2/"another one"/
   file = open("res.txt", "w")
    print(folder, file=file)
    docfile = models.FileField(upload_to="%s" % folder)
    file.close()
